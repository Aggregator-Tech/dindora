apply plugin: 'application'
mainClassName = "org.aggregatortech.dindora.topic.TopicApp"

dependencies {
    compile (
            project(':persistence-file'),
            project(':persistence-dynamodb')
    )
}